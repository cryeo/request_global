require 'spec_helper'

describe RequestGlobal do
  it 'has a version number' do
    expect(RequestGlobal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
